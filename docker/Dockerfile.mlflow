# MLflow tracking server image (server + Postgres driver). Artifacts are served by
# the server itself (--serve-artifacts), so clients only need to reach this service.
FROM python:3.12-slim
RUN pip install --no-cache-dir "mlflow==3.14.0" "psycopg2-binary"
EXPOSE 5000
